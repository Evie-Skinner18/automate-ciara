describe('My First Test', () => {
    it('Does not do much but I\'m just a legend HA!', () => {
      expect(true).to.equal(true)
    })
  })

  describe('My Failing Test', () => {
    it('Ooh ahh I\'m a naughty failing test', () => {
      expect(true).to.equal(false)
    })
  })